/**
 * Created by pc on 16/09/2015.
 */

/*
var should = require('should');
var Logics = require('../server/ontology/jsw/Logics');
var rule1;

describe('Rule creation', function () {
    it('should create a rule', function () {
        var fact1 = new Logics.fact('subClassOf', 'a', 'b'),
            fact2 = new Logics.fact('subClassOf', 'b', 'c'),
            fact3 = new Logics.fact('subClassOf', 'a', 'c');
        rule1 = new Logics.rule([fact1, fact2], fact3);
        rule1.should.exist;
    });

    it('should verify the satisfiability of a rule', function () {
        var facts = [
            new Logics.fact('subClassOf', '#2', '#3'),
            new Logics.fact('subClassOf', '#1', '#2'),
            new Logics.fact('subClassOf', '#4', '#6'),
            new Logics.fact('subClassOf', '#5', '#1'),
            new Logics.fact('subClassOf', '#7', '#5')
        ];
        var consequences = rule1.consequences(facts);
        consequences.length.should.equal(6);
    });
});*/
